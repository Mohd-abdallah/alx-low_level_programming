#include <stdio.h>

/**
 * main - a program that prints its name, followed by a new line.
 * @argc: input
 * @argv: array
 * Return: 0
*/
int main(int argc, char *argv[])
{
	int i;

	if (i == 6)
	{
		printf("%s", argv[i]);
	}
	else
	{
		i++;
	}
	return (0);
}
