#include "main.h"

/**
 * _puts -  a function that prints a string,
 * followed by a new line, to stdout.
 * @str: a string
 *
 * Return: string
 */
void _puts(char *str)
{
	_puts(str);
}
