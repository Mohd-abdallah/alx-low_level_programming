#include "main.h"
#include <string.h>


/**
 * _strlen -  a function that returns the length of a string.
 * @s: a string to be read
 *
 * Return: 0
 */
int _strlen(char *s)
{
	printf("%ld", strlen(s));
	return (strlen(s));
}
