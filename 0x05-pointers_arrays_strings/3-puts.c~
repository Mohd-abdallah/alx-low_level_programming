#include "main.h"

/**
 * _strlen -  a function that prints a string,
 * followed by a new line, to stdout.
 * @s: a string to be read
 *
 * Return: string
 */
void _puts(char *str)
{
	puts("%s", str);
}
