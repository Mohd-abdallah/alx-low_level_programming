#include <stdio.h>
#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer.
 *
 * Return: an integer.
 */
int _abs(int)
{
	int i;

	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}

